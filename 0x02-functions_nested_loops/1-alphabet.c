#include "main.h"
/**
 * main - prints alphabet
 * Description - use only _putchar
 *
 * Return: Always 0 (Success)
 */
int main(void)
{	char ch;
	for(ch = 'a'; ch <= 'z'; ch++)
		_putchar(ch);
	_putchar('\n');
	return (0);
}
