#include <stdio.h>
/**
 * main - prints the number of arguments passed into it
 * @argc: argument count
 * @argv: string arguments
 *
 * Return: 0 (Success)
 *
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
